// Фирменная типографика.
//
// Системный шрифт на iOS и Android разный, и приложение выглядело ничьим.
// Заголовки набираются Golos Text — гротеском с отличной кириллицей, — а
// основной текст остаётся системным: он лучше всего читается мелким кеглем
// и не тянет за собой лишние файлы начертаний.
//
// Пока шрифт не загрузился, React Native молча показывает системный — экраны
// не ждут и не мигают, поэтому загрузка не блокирует рендер (см. _layout).

import type { TextStyle } from 'react-native';

export const FONTS = {
  /** Крупные заголовки экранов и шторок */
  display: 'GolosText_700Bold',
  /** Подзаголовки и акцентные подписи */
  heading: 'GolosText_600SemiBold',
} as const;

/** Цифры одной ширины: цены и счётчики не «прыгают» при обновлении. */
export const TABULAR: Pick<TextStyle, 'fontVariant'> = { fontVariant: ['tabular-nums'] };
