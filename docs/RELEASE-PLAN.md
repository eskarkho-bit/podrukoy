# План выхода в App Store

Составлен 21.08.2026 по состоянию ветки `feature/marketplace-and-verification`.
Оценка готовности: **~60%**. Код продукта готов на ~85–90%, но между ним и
магазином стоят три блока работы, которых в коде не видно: боевая
инфраструктура Firebase, юридика по 152-ФЗ и сама публикация.

Правило работы с планом: сделали пункт — ставим галочку и коммитим. Пункты
внутри этапа идут в порядке выполнения; этапы 1–3 можно вести параллельно.

---

## Что уже готово (и почему это много)

- Полный цикл заявки: создание → предложения мастеров → выбор → работа →
  подтверждение → отзыв. Перехват чужих предложений невозможен по схеме данных.
- Проверка мастеров: анкета, фото лица, телефон, ручная модерация, сводка
  для модератора.
- Расчёты напрямую (06.09): реквизиты мастера, карточка «Оплата» у клиента,
  отметки «оплатил» / «получил» — без платёжного провайдера.
- Чат клиент–мастер после выбора исполнителя, пуш-уведомления (код готов).
- Удаление аккаунта из приложения с обезличиванием заявок — требование обоих
  магазинов выполнено.
- Согласия с версионированием, отдельное согласие на биометрию, отзыв согласия.
- 103 теста правил Firestore + 47 тестов функций + 69 тестов компонентов, CI.
- EAS-проект заведён, expo-updates настроен, иконки и фирменный стиль есть.

## Чего не хватает — по этапам

### Этап 1. Боевой Firebase (без него приложение — витрина)

Сейчас функции не развёрнуты и Storage не подключён: не приходят пуши, не
считается рейтинг, фото заявок остаются локальными
URI и другим устройствам не видны, серверное удаление аккаунта и сверка не
работают.

- [x] Включить тариф **Blaze** в проекте `domio-7ad1c` (сделано 29.08;
      бюджетный алерт в консоли — поставить, ещё висит).
- [x] Подключить **Storage** и выкатить его правила. (29.08: бакет в
      EUROPE-WEST3 — рядом с Firestore и по юридике; правила выкачены,
      cross-service роль для чтения Firestore из правил выдана.)
- [x] Включить **Cloud Scheduler API**. (29.08: включился сам при деплое;
      reconcile ходит каждые 15 минут, проверено по логам.)
- [x] Развернуть функции. (29.08: все 24, со второй попытки — триггерам
      Eventarc нужны минуты на права после первого включения API.)
- [x] Вернуть **TTL-политику** на `audit.expiresAt` и выкатить. (29.08;
      заодно всплыл и добавлен групповой индекс `verification/status` —
      без него в бою падала очередь модерации.)
- [x] Роль `iam.serviceAccountTokenCreator` сервисному аккаунту
      `380253738862-compute@developer.gserviceaccount.com` на самого себя —
      без неё вход по СМС не подпишет custom-токен.
- [x] Ключ `SMSRU_API_ID` и повторный деплой функций. (11.09: ключ
      перенесён из `functions/.env` в Secret Manager командой
      `firebase functions:secrets:set SMSRU_API_ID`; функция объявляет его
      в `secrets`, а переменные окружения видны всем с доступом к проекту.)
      (29.08: ключ рабочий, функция шлёт, но SMS.RU отвечает кодом 221 —
      буквенный отправитель оформляется только по договору на юрлицо/ИП.
      Обходной вариант реализован в тот же день: код доставляется звонком —
      робот звонит, код это последние 4 цифры звонящего номера, договор не
      нужен. После ИП и договора канал переключается на СМС переменной
      SMSRU_CHANNEL=sms без правки кода.)
- [x] Завести документ `admins/{uid}` для себя — без модератора ни один
      мастер не пройдёт проверку. (Есть: `dmtr0v@icloud.com`.)
- [x] Проверить `npm run rules:check` — правила в бою не отстали.
      (Выкачены 21.08, 29.08 и 11.09, паритет подтверждён.)

### Этап 2. Платежи

Решение 06.09.2026: **платёжного провайдера нет.** Расчёты идут напрямую —
наличными или переводом по СБП на телефон мастера; сервис показывает
клиенту, как мастер принимает оплату, и даёт обеим сторонам отметить
расчёт. Привязка карты через ЮKassa (тестовый контур прошёл 01.09) снесена
вместе с вебхуком и сверкой платежей: боевой магазин ждал ИП, а модель без
приёма денег юридически легче (LEGAL-ANALYSIS, §6) и проверена рынком.

- [x] Реквизиты мастера (банки для СБП, наличные), карточка «Оплата» у
      клиента, отметки «оплатил» / «получил», пуши и след в журнале,
      отметки в карточке заявки у модератора. (06.09)
- [x] При следующем деплое функций удалить с бою `createCardBinding` и
      `yookassaWebhook`, выкатить правила и индексы. (11.09: выкачено;
      сироты удалены, в бою 22 функции; индекс `bindingState` и
      переопределение `refundPending` из Firestore ушли.)
- [ ] В кабинете ЮKassa закрыть тестовый магазин 1453416. (`test_`-ключи
      из `functions/.env` убраны 11.09 — код их больше не читает.)
- [ ] Юристу: тексты соглашения и политики обновлены под расчёты напрямую
      (редакция 2026-09-06), при следующем входе все пользователи примут их
      заново — это штатно.
- [ ] Монетизация — отдельное решение. Комиссию с расчётов, идущих мимо
      сервиса, взять нельзя; остаются подписка или плата мастера за выбор,
      и это уже обычный платёж «мастер платит сервису» без сплитов и выплат.

### Этап 3. Юридика (самый длинный срок — начинать первым)

Подготовка сделана 21.08.2026 — юристу остаётся проверить и подписать:

- **проекты позиций по всем вопросам брифа** — `docs/LEGAL-ANALYSIS.md`
  (локализация: сценарий «первичная база в РФ»; биометрия и 572-ФЗ — три
  пути на выбор; агрегатор по ЗоЗПП 9.1 — пока не мы; пуши через США);
- **черновики уведомлений в РКН** и памятка при утечке —
  `docs/RKN-NOTIFICATION.md`;
- **план регистрации ИП** с ОКВЭД и порядком шагов — `docs/OPERATOR-SETUP.md`;
- **тексты документов** в `components/legal.ts` доведены до структуры
  152-ФЗ: основания по статьям, получатели, трансграничка (Франкфурт/США),
  сроки хранения, права субъекта; соглашение — до оферты (18+, заверения
  мастера, претензионный порядок);
- **веб-страницы документов** генерируются из тех же текстов:
  `node scripts/legal-site.mjs` → `firebase deploy --only hosting`;
- в форме заявки появилось предупреждение, что адрес и фото увидят
  проверенные мастера города.

Остались действия, которые делает человек:

- [ ] Зарегистрировать **ИП** (по `OPERATOR-SETUP.md`; нужно для оферты и
      уведомлений в РКН).
- [ ] Вписать реквизиты в `legal.ts` (`OPERATOR`, `CONTACT`), поднять
      версии документов, перегенерировать страницы, выложить хостинг.
- [x] Отдать юристу `LEGAL-BRIEF.md` + `LEGAL-ANALYSIS.md` на проверку.
      (01.09: юрист дал добро на запуск — «выпускать, потом исправим».
      Хвосты: порог «инцидента» при утечке и договор поручения с SMS.RU.)
      Два блокирующих решения: **фото лица (биометрия/572-ФЗ)** и
      **срок реализации локализации**.
- [ ] Подать оба уведомления в Роскомнадзор (черновики готовы), по США —
      дождаться истечения 10 рабочих дней. После подачи — вписать в политику
      фразу о направленных уведомлениях (пометка в `legal.ts`, раздел 6).
- [x] Включить TTL-политику журнала `audit`: политика обещает хранение не
      более 400 дней. (Сделано 29.08 вместе с этапом 1 — TTL в
      `firestore.indexes.json` и выкачена.)
- [ ] До публичного релиза: реализовать «первичную базу в РФ»
      (LEGAL-ANALYSIS.md, раздел 1, сценарий Б), если юрист подтвердит.

### Этап 4. Доводка приложения до витрины

Выполнен 21.08.2026:

- [x] **Сплэш-экран**: нативный настроен (`expo-splash-screen` в `app.json`,
      знак на фоне темы, тёмный вариант) — он показывается до загрузки JS и
      передаёт эстафету анимированной заставке с башней.
- [x] **Крашрепорты**: установлен `@sentry/react-native`; включается через
      `EXPO_PUBLIC_SENTRY_DSN` в `.env` (`components/crashReporting.ts`),
      без ключа честно молчит, в Expo Go не инициализируется.
      Осталось: завести аккаунт Sentry и вписать DSN перед сборкой в стор.
- [x] **Живая поддержка**: заготовленные ответы убраны — тред поддержки
      читает модератор и отвечает из раздела модерации; пуши об обращениях
      и ответах шлёт сервер (`onSupportMessageCreated`).
- [x] `ITSAppUsesNonExemptEncryption: false` в `app.json`.
- [x] Прибраться: `podrukoy2/` и `firestore-debug.log` удалены.
- [x] Все семь проверок зелёные, ветка слита в `main`.

### Этап 4а. Неделя 12.09: сборка и дыры, найденные аудитом

Аудит 11.09 прошёл по коду всеми тремя ролями и нашёл, что заметил бы
ревьюер магазина или первый же клиент. Закрыто 12.09:

- [x] Slug в `app.json` возвращён к `podrukoy`: под этим именем заведён
      проект EAS, а slug у проекта не меняется (expo.fyi/eas-project-id).
      Имя приложения по-прежнему domio. Проект с именем domio — только
      новый `eas init` и новый `updates.url`; решать, когда будут первые
      реальные пользователи.
- [x] Preview-сборка Android на EAS. Первая упала на плагине Sentry:
      без организации и токена он роняет gradle на загрузке source maps.
      В `eas.json` во всех профилях `SENTRY_DISABLE_AUTO_UPLOAD=true` —
      убрать, когда появится аккаунт Sentry (см. ниже).
- [x] Клиент возвращает работу мастеру с приёмки («Ещё не готово»).
- [x] Мастер отказывается от взятой заявки — сервер возвращает её в поиск.
- [x] Проверенный мастер правит телефон, «о себе» и фото; телефон и фото
      уходят на повторную проверку с снятием допуска.
- [x] Жалоба на мастера и на сообщение в чате, блокировка мастера клиентом
      с списком в профиле — требование магазинов к пользовательскому
      контенту.
- [x] Тумблер «Push-уведомления» настоящий (`pushOff`, слушает сервер);
      тумблер email-уведомлений убран — их нет.
- [x] Правила: `createdAt` заявки только серверным временем, длины
      заголовка, комментария и адреса ограничены; `setGlobalOptions`
      вынесен в `options.ts` и действует на все 22 функции.
- [x] `microphonePermission: false` у image-picker: видео не снимаем.
- [ ] **Sentry** — аккаунт заводит владелец: sentry.io → организация и
      проект React Native → DSN. Дальше: `EXPO_PUBLIC_SENTRY_DSN` в
      переменные окружения EAS (`eas env:create`), `organization` и
      `project` в плагин `@sentry/react-native` в `app.json`,
      `SENTRY_AUTH_TOKEN` секретом EAS, и убрать
      `SENTRY_DISABLE_AUTO_UPLOAD` из `eas.json`.
- [ ] **FCM для Android**: без ключа сервисного аккаунта пуши на Android
      не доставляются даже с рабочими функциями. Firebase Console →
      Project settings → Service accounts → Generate new private key,
      затем `eas credentials` → Android → Google Service Account Key for
      Push Notifications (FCM V1) → загрузить файл.
- [ ] Прогон preview-APK на телефоне: вход по телефону, камера и селфи в
      анкете, пуши (после FCM-ключа), отметки оплаты, удаление аккаунта.

### Этап 5. Сборка и публикация

- [ ] **Apple Developer Program** — $99/год, оформление до недели.
- [ ] Создать приложение в **App Store Connect** (bundle `com.domio.app`),
      вписать `ascAppId` в `eas.json` → `submit.production`.
- [ ] `eas credentials` — сертификаты и **APNs-ключ** (без него пуши на iOS
      не пойдут даже с развёрнутыми функциями).
- [ ] `eas build --profile production --platform ios` и прогон на TestFlight
      на живом устройстве: пуши, камера, отметки оплаты, удаление аккаунта.
- [ ] Витрина: название, подзаголовок, описание, ключевые слова, скриншоты
      (6.7" и 6.1" обязательны), URL политики, контакт поддержки.
- [ ] Анкета **App Privacy** («nutrition labels»): собираем имя, email,
      адреса, фото, телефон, фото лица — заполнять честно, Apple сверяет.
- [ ] **Демо-доступ для ревьюера**: тестовый клиент + тестовый одобренный
      мастер + краткая записка, как устроена проверка мастеров. Ревьюеры
      заваливают приложения, в которых не смогли пройти основной сценарий.
- [ ] `eas submit --platform ios` → ревью (обычно 1–3 дня).

### Этап 6. После запуска (не блокирует публикацию)

- [ ] Google Play / RuStore — Android-сборка уже профилирована в `eas.json`.
- [x] Экспорт данных пользователя (обещан в LEGAL-BRIEF как запланированный).
      (Сделано 29.08: «Скачать мои данные» в профиле — JSON со всем
      владельческим, фото ссылками, служебные токены вырезаны.)
- [ ] Открыть остальные города (сейчас включён один Грозный — правильно для
      старта: пустой маркетплейс хуже маленького).
- [ ] Счётчики заявок для модератора через функции (см. ARCHITECTURE).
- [ ] Онбординг мастеров вручную: до публикации найти 5–10 мастеров в
      Грозном, иначе первый клиент увидит тишину.

---

## Советы

1. **Начинайте с этапа 3 сегодня.** Регистрация ИП, юрист и Роскомнадзор —
   недели, всё остальное — дни. Технические этапы 1–2 делаются параллельно.
2. **Вопрос локализации данных решите до, а не после публикации.** Если юрист
   скажет «переносить в РФ», это смена бэкенда — лучше знать это раньше, чем
   после жалобы.
3. **Не открывайте все города сразу.** Маркетплейс жив, когда на заявку
   отвечают. Один город с десятком мастеров лучше республики с нулём.
4. **TestFlight-период не сжимайте.** Пуши и камера проверяются только на
   живом устройстве с боевыми функциями — это первый момент, когда вся
   цепочка соберётся вместе.
5. **Ревью Apple**: приложения с «проверкой людей» смотрят внимательно.
   Записка ревьюеру про то, что платежей в приложении нет вовсе, а оплата
   работ идёт вне приложения между сторонами, снимет большинство вопросов
   заранее.
